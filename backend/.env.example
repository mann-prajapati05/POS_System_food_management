# PostgreSQL Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=odoo_pos

# Node Environment
NODE_ENV=development

# Optional: Socket.io port (for real-time events)
SOCKET_PORT=3001
